# GRACE 4 bootstrap record

Worldline adopted the canonical GRACE 4 artifact model on 2026-08-28.

- `.grace/` is the only durable GRACE source of truth.
- No legacy GRACE 3 `docs/*.xml` artifacts were present or converted.
- Existing source changes and README references that predate this bootstrap
  are historical context, not retroactively manufactured change bundles.
- New governed work starts with a `C-*` spec and a separately approved plan.
