[package]
name = "memory-hog"
version = "0.1.0"
edition = "2021"
publish = false


[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.61", default-features = false, features = ["macros"] }
dlmalloc = { version = "0.2", features = ["global"] }
