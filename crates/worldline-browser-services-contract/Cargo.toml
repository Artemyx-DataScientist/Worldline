[package]
name = "worldline-browser-services-contract"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Engine-neutral experimental capability contracts for Worldline browser services (tabs, history)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
worldline-browser-contract = { path = "../worldline-browser-contract" }
