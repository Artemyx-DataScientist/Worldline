[package]
name = "worldline-browser-provider"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Browser engine provider core state machine, CAS generation protocol, and reference engine backend"

[dependencies]
worldline-browser-contract = { path = "../worldline-browser-contract" }
worldline-plugin-protocol = { path = "../worldline-plugin-protocol" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
