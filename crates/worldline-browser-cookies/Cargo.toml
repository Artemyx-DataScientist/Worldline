[package]
name = "worldline-browser-cookies"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Browser cookies and site-data service plugin for Worldline managing metadata inspection, secret redaction, and scope isolation"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
worldline-browser-contract = { path = "../worldline-browser-contract" }
worldline-browser-services-contract = { path = "../worldline-browser-services-contract" }
worldline-kernel = { path = "../worldline-kernel" }
url = "2"
